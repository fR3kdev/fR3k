# Build Window Marker

This repository begins with design/specification only.

No contest implementation source is intentionally included in this initial commit. The implementation commits should begin only after the official hackathon build window opens.

That separation is deliberate so the public history itself documents a clean pre-build specification followed by the live build.
